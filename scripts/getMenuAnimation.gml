/// getMenuAnimation()

// ? refactor all these into single function?
// ? have all functions but use same one internally? feels a bit too overengineered

var currentMenu = menuController.menus[? menuController.currentMenu];
return currentMenu[? "animation"];

